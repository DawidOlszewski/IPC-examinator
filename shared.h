typedef enum serverEventType { //events that were sent by a server
    Question,
    Time,
} serverEventType;

typedef enum clientEventType { //events that were sent by a server
    Answer
} clientEventType;